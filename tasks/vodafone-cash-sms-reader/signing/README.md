# Signing Keystore

This keystore is used to sign the release APK so that Android updates work over existing installations.

- Keystore: `release.keystore`
- Alias: `naderai`
- Passwords: `naderai2024`
- Keep this file safe; losing it means future updates cannot be installed over existing APKs.
