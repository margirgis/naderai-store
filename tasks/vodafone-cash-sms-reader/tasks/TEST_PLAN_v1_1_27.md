# خطة الاختبار — v1.1.27

## أولاً: اختبارات Web UI (AdminTopupRequestsPage)

### T-W1: عرض Dashboard Stats
- [ ] افتح صفحة الطلبات
- [ ] تحقق أن الأرقام (فحص / تأكيد / فشل / منتهي / اتصال / أُعيد / الكل) تتطابق مع قاعدة البيانات
- [ ] تحقق أن شريط حالة الجهاز يعرض ✅ متصل عند وجود heartbeat حديث

### T-W2: Realtime بدون Refresh
- [ ] افتح الصفحة في المتصفح
- [ ] أرسل طلب شحن جديد من الجهاز
- [ ] تحقق أن البطاقة تظهر تلقائياً في الأعلى مع toast "📥 طلب شحن جديد"
- [ ] تحقق أن حالة الطلب تتحدث تلقائياً عند تغيرها

### T-W3: بطاقات الحالات المختلفة
- [ ] `pending` → pill أصفر + نص "قيد الانتظار"
- [ ] `scanning` → pill أزرق متحرك + نص "جاري الفحص"
- [ ] `admin_offline` → pill برتقالي + تحذير مضمّن
- [ ] `expired` → pill رمادي + زر "إعادة فتح الطلب"
- [ ] `reopened` → pill بنفسجي
- [ ] `confirmed/approved` → pill أخضر

### T-W4: التأكيد اليدوي (admin-manual-confirm)
- [ ] اختر طلباً في حالة `scanning` مع `payment_order_id`
- [ ] اضغط "تأكيد يدوي"
- [ ] تحقق أن الكريدت أُضيف للعميل فعلاً
- [ ] تحقق أن حالة الطلب صارت `confirmed`
- [ ] حاول تأكيد نفس الطلب مرة ثانية → يجب أن يرفض

### T-W5: إعادة فتح طلب منتهي
- [ ] اختر طلباً `expired` مع `payment_order_id`
- [ ] اضغط "إعادة فتح الطلب"
- [ ] تحقق أن حالته صارت `reopened`
- [ ] تحقق أن `expires_at` تمدد بـ 30 دقيقة
- [ ] تحقق أن `wallet_topup_request` جديد أُنشئ

### T-W6: فتح قضية
- [ ] اختر طلباً غير مكتمل مع `payment_order_id`
- [ ] اضغط "فتح قضية" → أدخل السبب
- [ ] تحقق أن سجلاً أُضيف في جدول `support_cases`
- [ ] حاول فتح قضية ثانية → يجب أن يرفض بـ "قضية مفتوحة بالفعل"

### T-W7: Admin Offline Banner
- [ ] أوقف تشغيل الجهاز
- [ ] انتظر 6 دقائق
- [ ] تحقق أن البانر يتحول لـ 🔴 مع عدد الطلبات المنتظرة

### T-W8: Filters
- [ ] `قيد المراجعة` → يعرض `pending` + `scanning` فقط
- [ ] `منتهي` → يعرض `expired` فقط
- [ ] `أُعيد فتحه` → يعرض `reopened` فقط
- [ ] `جميع الطلبات` → يعرض الكل

---

## ثانياً: اختبارات Android

### T-A1: حالة ADMIN_OFFLINE
- [ ] أوقف التطبيق
- [ ] أرسل طلب شحن من الويب
- [ ] شغّل التطبيق — تحقق أن الطلب يظهر بحالة `ADMIN_OFFLINE` مع أيقونة 📵
- [ ] تحقق أن الرسالة "الجهاز غير متصل — سيُفحص عند الاتصال" تظهر في scanProgress
- [ ] تحقق أن زر "تأكيد يدوي" يظهر

### T-A2: حالة REOPENED
- [ ] افتح طلباً بحالة `REOPENED` من الويب
- [ ] تحقق أنه يظهر في Android بأيقونة 🔓 ولون بنفسجي
- [ ] تحقق أن زر "ابدأ الفحص" يظهر

### T-A3: التأكيد اليدوي عبر Edge Function
- [ ] اختر طلباً بحالة `scanning` مع `paymentOrderId` != null
- [ ] اضغط "تأكيد يدوي"
- [ ] تحقق أن الطلب يُرسل إلى `admin-manual-confirm` (راقب LogCat)
- [ ] تحقق أن الكريدت أُضيف للعميل
- [ ] **لا** يجب أن يُرسل `transactionId = "manual-{id}-{timestamp}"`

### T-A4: Fallback للطلبات القديمة
- [ ] اختر طلباً قديماً بدون `paymentOrderId`
- [ ] اضغط "تأكيد يدوي"
- [ ] تحقق أن المسار القديم يُستخدم (task-result webhook)

### T-A5: fromString للحالات الجديدة
- [ ] تحقق أن "admin_offline" يُحوَّل لـ `ADMIN_OFFLINE`
- [ ] تحقق أن "waiting_for_verification" يُحوَّل لـ `WAITING_FOR_VERIFICATION`
- [ ] تحقق أن "reopened" يُحوَّل لـ `REOPENED`

---

## ثالثاً: اختبارات قاعدة البيانات

### T-DB1: Atomic Credit (admin_manual_confirm_order)
- [ ] طلب صحيح → credit يُضاف مرة واحدة فقط
- [ ] transaction_id مكرر → يُرفض برسالة واضحة
- [ ] مبلغ مختلف → يُرفض (600.50 ≠ 600.05)
- [ ] طلب `confirmed` بالفعل → يُرفض

### T-DB2: Reopen (reopen_payment_order)
- [ ] طلب `expired` → يصبح `reopened` + `expires_at` يزيد 30 دقيقة
- [ ] طلب `confirmed` → يُرفض (لا يمكن إعادة فتح مكتمل)

### T-DB3: Support Cases
- [ ] أول قضية → تُنشأ بنجاح
- [ ] قضية ثانية على نفس الطلب → ترفض بـ `case_already_open`

---

## معايير نجاح الإصدار

| # | السيناريو | النتيجة المتوقعة |
|---|---|---|
| 1 | تحويل صحيح (المبلغ مطابق، tx_id جديد) | credit يُضاف، حالة = confirmed |
| 2 | تحويل بمبلغ مختلف | يُرفض، admin_offline أو no_match |
| 3 | tx_id مكرر | يُرفض بـ duplicate |
| 4 | جهاز مغلق | حالة التحقق = admin_offline |
| 5 | إعادة فتح طلب منتهي | reopened + extends 30min |
| 6 | تأكيد يدوي من Web UI | credit يُضاف مرة واحدة |
| 7 | تأكيد يدوي من Android | يُرسل EF (لا fake tx_id) |
| 8 | فتح قضية مزدوجة | يُرفض |
| 9 | Realtime update في Web | يتحدث بدون refresh |
| 10 | Dashboard stats | تتطابق مع قاعدة البيانات |
